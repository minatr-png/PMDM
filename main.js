document.addEventListener('DOMContentLoaded', () =>{
    changeTitle();
});

const changeTitle = () => {
    document.title = "Ahora trabajando con DOM dinámico";
}

const addID = () => {
    return 
}